# Flutter Rust Demo 项目架构以及规划信息

## 项目概览
- **项目名称**: Flutter Rust Demo
- **项目类型**: 即时通讯应用
- **技术栈**: Flutter + Rust
- **核心功能**: 聊天、通讯录、个人信息管理、群组功能

## 技术组件选型

### 核心框架
| 组件 | 版本 | 用途 | 选型理由 | 状态 |
|------|------|------|----------|------|
| Flutter | 3.16.0+ | 跨平台UI框架 | 高性能、跨平台支持、热重载、丰富的UI组件库 | ✅ 已使用 |
| Rust | 1.75.0+ | 高性能后端逻辑 | 内存安全、无垃圾回收、高性能、跨平台支持 | ✅ 已使用 |
| Flutter Rust Bridge | 1.80.0+ | Dart与Rust通信桥接 | 自动生成绑定代码、类型安全、性能优化 | ✅ 已使用 |

### 状态管理
| 组件 | 版本 | 用途 | 选型理由 | 状态 |
|------|------|------|----------|------|
| Riverpod | 2.4.0+ | 状态管理 | 当前最主流，依赖注入、可测试性、无Context依赖、性能优化 | 🔄 待迁移 |
| ChangeNotifier | 内置 | 临时状态管理 | 当前正在使用，后续迁移至 Riverpod | ✅ 已使用 |

### 路由管理
| 组件 | 版本 | 用途 | 选型理由 | 状态 |
|------|------|------|----------|------|
| go_router | 14.0.0+ | 导航路由 | 官方推荐，声明式路由配置、嵌套路由、路由守卫、参数传递、类型安全 | ✅ 已使用 |

### 网络与通信
| 组件 | 版本 | 用途 | 选型理由 | 状态 |
|------|------|------|----------|------|
| Dio | 5.4.0+ | 网络请求 | 最流行 HTTP 库，强大的HTTP客户端、拦截器支持、请求取消、超时处理 | 🔄 待迁移 |
| WebSocket | 内置 | 实时通信 | 支持长连接、实时消息推送、低延迟 | ✅ 已使用 |

### 数据模型与JSON解析
| 组件 | 版本 | 用途 | 选型理由 | 状态 |
|------|------|------|----------|------|
| Freezed | 2.4.0+ | 数据类生成 | 不可变数据类、copyWith、代码生成 | 🔄 待迁移 |
| json_serializable | 6.7.0+ | JSON解析 | 自动 JSON 转换，与 Freezed 配合使用 | 🔄 待迁移 |

### 本地存储与数据库
| 组件 | 版本 | 用途 | 选型理由 | 状态 |
|------|------|------|----------|------|
| Hive | 2.2.0+ | 轻量数据库 | 轻量级、高性能、跨平台、无需SQL | 🔄 待迁移 |
| Drift | 2.10.0+ | SQLite ORM | 类型安全、响应式查询、代码生成 | 🔄 待迁移 |
| shared_preferences | 2.2.0+ | 键值存储 | 简单配置存储、跨平台支持、易用性高 | ✅ 已使用 |

### UI组件库
| 组件 | 版本 | 用途 | 选型理由 | 状态 |
|------|------|------|----------|------|
| flutter_screenutil | 5.9.0+ | 屏幕适配 | UI组件库，屏幕尺寸适配、响应式布局 | 🔄 待迁移 |
| Material Design | 内置 | UI设计系统 | 谷歌官方设计语言、丰富的组件库、跨平台一致性 | ✅ 已使用 |
| Cupertino | 内置 | iOS风格UI | 适配iOS平台、提供原生iOS体验 | ✅ 已使用 |
| flutter_svg | 2.0.0+ | SVG图标支持 | 矢量图标、缩放不失真、减小应用体积 | 🔄 待迁移 |
| cached_network_image | 3.3.0+ | 网络图片缓存 | 性能优化、离线访问、内存管理 | 🔄 待迁移 |

### 推送服务
| 组件 | 版本 | 用途 | 选型理由 | 状态 |
|------|------|------|----------|------|
| Firebase Cloud Messaging | 14.0.0+ | 推送通知 | 跨平台支持、可靠性高、免费使用 | 🔄 待迁移 |
| OneSignal | 5.0.0+ | 推送通知备选 | 丰富的推送功能、分析能力、多平台支持 | ⏸️ 备选 |

### 工具库
| 组件 | 版本 | 用途 | 选型理由 | 状态 |
|------|------|------|----------|------|
| get_it | 7.6.0+ | 依赖注入 | DI 容器，服务定位器模式，是否启用需讨论待定 | ⏸️ 待定 |
| logger | 2.0.0+ | 日志管理 | 开发日志，分级日志、格式化输出、多平台支持 | 🔄 待迁移 |
| dartz | 0.10.1+ | 函数式编程 | Result类型、错误处理、函数式操作 | 🔄 待迁移 |
| path_provider | 2.1.0+ | 文件路径管理 | 跨平台文件系统访问、目录管理 | ✅ 已使用 |
| permission_handler | 11.0.0+ | 权限管理 | 统一权限请求、跨平台支持、易用性高 | 🔄 待迁移 |
| image_picker | 1.0.0+ | 图片选择 | 相机/相册访问、跨平台支持、易用性高 | 🔄 待迁移 |
| video_player | 2.7.0+ | 视频播放 | 跨平台视频播放、控制功能丰富 | 🔄 待迁移 |
| audio_recorder | 0.11.0+ | 音频录制 | 语音消息支持、跨平台兼容 | 🔄 待迁移 |

### 开发工具
| 组件 | 版本 | 用途 | 选型理由 | 状态 |
|------|------|------|----------|------|
| flutter_lints | 3.0.0+ | 代码规范检查 | 统一代码风格、减少错误、提高可维护性 | 🔄 待迁移 |
| build_runner | 2.4.0+ | 代码生成 | 自动生成序列化代码、减少手动编码 | 🔄 待迁移 |
| mockito | 5.4.0+ | 测试模拟 | 单元测试支持、提高测试覆盖率 | 🔄 待迁移 |
| integration_test | 内置 | 集成测试 | 端到端测试、验证应用功能 | ✅ 已使用 |

## 目录结构
```
lib/
├── models/          # 数据模型
├── screens/         # 屏幕页面
├── services/        # 业务服务
├── src/rust/        # Rust 核心代码
├── theme/           # 主题配置
├── utils/           # 工具类
├── widgets/         # 可复用组件
└── main.dart        # 应用入口
```

## 核心模块
| 模块 | 职责 | 文件路径 |
|------|------|----------|
| 数据模型 | 定义应用数据结构 | lib/models/ |
| 屏幕页面 | 实现用户界面 | lib/screens/ |
| 业务服务 | 处理核心业务逻辑 | lib/services/ |
| Rust 集成 | 提供高性能核心功能 | lib/src/rust/ |
| 主题配置 | 管理应用样式 | lib/theme/ |
| 工具类 | 提供通用功能 | lib/utils/ |
| 可复用组件 | 封装 UI 组件 | lib/widgets/ |

## 主要屏幕
| 屏幕名称 | 功能描述 | 文件路径 |
|---------|---------|---------|
| SplashScreen | 启动屏，包含自动登录逻辑 | lib/screens/splash_screen.dart |
| LoginScreen | 登录页面 | lib/screens/login_screen.dart |
| MainScreen | 主页面，包含底部导航栏 | lib/screens/main_screen.dart |
| ChatListScreen | 会话列表页面 | lib/screens/chat_list_screen.dart |
| ChatDetailScreen | 聊天详情页面 | lib/screens/chat_detail_screen.dart |
| ContactsScreen | 通讯录页面 | lib/screens/contacts_screen.dart |
| DiscoverScreen | 发现页面 | lib/screens/discover_screen.dart |
| MyProfileScreen | 个人信息编辑页面 | lib/screens/my_profile_screen.dart |
| ProfileScreen | 个人中心页面 | lib/screens/profile_screen.dart |
| ProfileDrawerScreen | 侧边抽屉个人信息页面 | lib/screens/profile_drawer_screen.dart |
| SearchScreen | 搜索页面 | lib/screens/search_screen.dart |
| GroupInfoScreen | 群组信息页面 | lib/screens/group_info_screen.dart |
| ChatSettingsScreen | 聊天设置页面 | lib/screens/chat_settings_screen.dart |
| UserProfileScreen | 用户资料页面 | lib/screens/user_profile_screen.dart |

## 核心服务
| 服务名称 | 功能描述 | 文件路径 |
|---------|---------|---------|
| MessageService | 消息服务，管理客户端连接、会话、消息 | lib/services/message_service.dart |
| AuthApi | 认证 API，处理登录、注册等认证相关操作 | lib/services/auth_api.dart |

## 状态管理
- **ChangeNotifier**：MessageService 继承自 ChangeNotifier，用于管理应用状态
- **setState**：在各个 StatefulWidget 中使用 setState 管理局部状态
- **Stream**：通过 Stream 监听 Rust 端的事件

## Rust 集成
项目通过 Flutter Rust Bridge 集成 Rust 代码，实现了核心功能的高性能计算和跨平台支持。

**主要 Rust 模块**：
- **API**：提供 Dart 调用 Rust 的接口
- **IM**：即时通讯相关功能
- **模型**：定义数据模型

## 当前项目状态
### 已实现功能
- ✅ 启动屏与自动登录
- ✅ 登录功能
- ✅ 会话列表展示
- ✅ 聊天功能
- ✅ 个人信息编辑
- ✅ 用户资料展示
- ✅ 通讯录功能
- ✅ 搜索功能

### 存在的问题
#### UI 组织问题
- 部分屏幕代码过于复杂，组件化程度不足
- 状态管理分散，使用 setState 较多
- 导航逻辑分散在各个屏幕中
- 样式管理不够集中

#### 逻辑组织问题
- MessageService 职责过重，包含太多功能
- 数据层抽象不足，直接使用服务层数据
- 错误处理机制不够统一
- 测试覆盖不足
- Rust 代码模块化程度有待提高

## 详细改进事项与理由
### UI 问题改进事项
| 改进事项 | 具体描述 | 改进理由 | 优先级 |
|---------|---------|---------|--------|
| 拆分 ChatListScreen | 将 ChatListScreen 拆分为 ChatListHeader、ChatListItem、GroupFilterPanel 等组件 | 1. 提高代码可读性和可维护性<br>2. 便于单独测试和复用组件<br>3. 减少单个文件的代码量 | 高 | 已完成 |
| 拆分 ChatDetailScreen | 将 ChatDetailScreen 拆分为 MessageList、ChatInput、MessageBubble 等组件 | 1. 提高代码可读性和可维护性<br>2. 便于单独测试和复用组件<br>3. 减少单个文件的代码量 | 高 | 已完成 |
| 创建布局组件 | 提取通用布局结构，如 CardLayout、ListRow 等 | 1. 统一布局风格<br>2. 减少重复代码<br>3. 便于维护和修改 | 中 |
| 引入 Riverpod 状态管理 | 替换 ChangeNotifier，使用 Riverpod 管理核心状态 | 1. 更好的状态管理方式<br>2. 支持依赖注入<br>3. 提高代码可测试性 | 高 |
| 实现命名路由 | 在 MaterialApp 中配置命名路由，替代直接使用 Navigator.push | 1. 集中管理导航逻辑<br>2. 提高代码可读性<br>3. 便于修改和维护 | 高 |
| 创建 NavigationService | 封装导航逻辑，提供统一的导航方法 | 1. 集中管理导航逻辑<br>2. 减少重复代码<br>3. 便于测试和模拟 | 高 |
| 扩展 ThemeData | 添加自定义主题属性，统一管理样式 | 1. 集中管理样式<br>2. 便于主题切换<br>3. 提高代码一致性 | 中 |
| 提取样式常量 | 将常用样式提取为常量，确保 UI 风格一致 | 1. 统一样式管理<br>2. 便于修改和维护<br>3. 提高代码一致性 | 中 |
| 实现响应式布局 | 使用 MediaQuery 和 LayoutBuilder 实现响应式布局 | 1. 适配不同屏幕尺寸<br>2. 提高用户体验<br>3. 支持多设备 | 中 |

### 组织问题改进事项
| 改进事项 | 具体描述 | 改进理由 | 优先级 |
|---------|---------|---------|--------|
| 拆分 MessageService | 将 MessageService 拆分为 AuthService、ChatService、UserService、ConnectionService | 1. 职责分离，提高代码可维护性<br>2. 便于单独测试和复用<br>3. 减少单个服务的复杂度 | 高 |
| 实现依赖注入 | 使用 Riverpod 或 GetIt 实现服务的依赖注入 | 1. 提高代码可测试性<br>2. 便于服务替换和模拟<br>3. 减少硬编码依赖 | 高 |
| 添加 Repository 层 | 封装数据获取和处理逻辑，提供统一的数据访问接口 | 1. 抽象数据访问逻辑<br>2. 便于切换数据源<br>3. 提高代码可测试性 | 高 |
| 分离 UI 模型与数据模型 | 使用映射器在 UI 模型与数据模型之间转换 | 1. 解耦 UI 与数据层<br>2. 提高代码可维护性<br>3. 便于 UI 模型的定制 | 中 |
| 创建 Result 类型 | 实现 Result 类型，统一处理异步操作的错误 | 1. 统一错误处理机制<br>2. 提高代码可读性<br>3. 便于错误传递和处理 | 高 |
| 添加全局错误处理 | 实现全局错误捕获和处理机制 | 1. 统一错误处理逻辑<br>2. 提高应用稳定性<br>3. 改善用户体验 | 高 |
| 编写单元测试 | 为核心服务和组件编写单元测试 | 1. 提高代码质量<br>2. 便于代码重构<br>3. 减少回归 bug | 中 |
| 编写集成测试 | 验证不同模块之间的交互 | 1. 确保模块间交互正常<br>2. 提高应用稳定性<br>3. 减少集成 bug | 中 |
| 编写 Widget 测试 | 测试 UI 组件的渲染和交互 | 1. 确保 UI 组件正常工作<br>2. 便于 UI 组件的重构<br>3. 减少 UI bug | 中 |
| 模块化 Rust 代码 | 将 Rust 代码按功能模块组织 | 1. 提高 Rust 代码可维护性<br>2. 便于 Rust 代码的测试<br>3. 减少 Rust 代码的复杂度 | 中 |
| 优化 Dart-Rust 通信 | 减少频繁的跨语言调用，批量处理操作 | 1. 提高应用性能<br>2. 减少通信开销<br>3. 改善用户体验 | 中 |
| 配置 lint 工具 | 使用 flutter_lints 或 custom_lints 确保代码风格一致 | 1. 统一代码风格<br>2. 提高代码可读性<br>3. 减少代码错误 | 中 | 已完成 |
| 添加文档注释 | 为公共 API 和关键逻辑添加详细的文档注释 | 1. 提高代码可读性<br>2. 便于团队协作<br>3. 减少维护成本 | 低 |
| 优化文件组织 | 按功能模块组织文件，遵循单一职责原则 | 1. 提高代码可维护性<br>2. 便于代码导航<br>3. 减少文件复杂度 | 低 |

## 实施规划
### 短期规划（1-2 周）
| 任务 | 描述 | 优先级 |
|------|------|--------|
| 导航优化 | 集成 go_router 实现声明式路由配置 | 高 |
| 组件拆分 | 将复杂屏幕拆分为更小的可复用组件 | 高 |
| 状态管理优化 | 引入 Riverpod 或 Bloc 管理核心状态 | 高 |
| 主题优化 | 扩展 ThemeData，统一样式管理 | 中 |
| 代码规范 | 配置 lint 工具，确保代码风格一致 | 中 |

### 中期规划（2-4 周）
| 任务 | 描述 | 优先级 |
|------|------|--------|
| 服务层拆分 | 将 MessageService 拆分为多个专注服务 | 高 |
| 数据层抽象 | 添加 Repository 层，实现数据仓库抽象 | 高 |
| 错误处理 | 实现 Result 类型和全局错误处理 | 高 |
| 测试覆盖 | 为核心功能添加单元测试 | 中 |
| Rust 代码优化 | 模块化 Rust 代码，提高可维护性 | 中 |

### 长期规划（1-2 个月）
| 任务 | 描述 | 优先级 |
|------|------|--------|
| 响应式设计 | 优化应用在不同屏幕尺寸下的表现 | 中 |
| 集成测试 | 编写集成测试，验证模块间交互 | 中 |
| Widget 测试 | 测试 UI 组件的渲染和交互 | 中 |
| 性能优化 | 优化 Dart-Rust 通信，提高响应速度 | 中 |
| 文档完善 | 为项目添加详细的文档 | 低 |

## 预期效果
1. **代码质量提升**：
   - 代码结构更清晰，职责更明确
   - 可维护性和可扩展性提高
   - 测试覆盖率增加，代码可靠性提升

2. **性能优化**：
   - 状态管理更高效，减少不必要的重建
   - Rust 与 Dart 通信更优化，提高响应速度
   - 组件化设计减少重复渲染，提升 UI 性能

3. **开发效率提升**：
   - 模块化设计便于团队协作
   - 统一的导航和状态管理减少开发复杂度
   - 完善的错误处理机制减少调试时间

4. **用户体验改善**：
   - 响应式设计适配不同设备
   - 界面风格一致，交互流畅
   - 错误处理更友好，提升用户满意度

## 与业界 IM 应用对比分析
### 功能层面缺失分析
#### 核心聊天功能
| 功能 | 业界 IM 应用（如微信、QQ、Slack） | 当前项目状态 | 建议实现优先级 |
|------|--------------------------------|-------------|----------------|
| 消息撤回 | ✅ 支持 | ❌ 未实现 | 高 |
| 消息转发 | ✅ 支持 | ❌ 未实现 | 高 |
| 消息引用 | ✅ 支持 | ❌ 未实现 | 中 |
| 消息已读回执 | ✅ 支持 | ❌ 未实现 | 中 |
| 消息置顶 | ✅ 支持 | ❌ 未实现 | 中 |
| 免打扰设置 | ✅ 支持 | ❌ 未实现 | 中 |
| 消息草稿 | ✅ 支持 | ❌ 未实现 | 低 |

#### 多媒体功能
| 功能 | 业界 IM 应用 | 当前项目状态 | 建议实现优先级 |
|------|-------------|-------------|----------------|
| 图片发送 | ✅ 支持 | ❌ 未实现 | 高 |
| 视频发送 | ✅ 支持 | ❌ 未实现 | 高 |
| 语音消息 | ✅ 支持 | ❌ 未实现 | 高 |
| 文件传输 | ✅ 支持 | ❌ 未实现 | 中 |
| 语音通话 | ✅ 支持 | ❌ 未实现 | 中 |
| 视频通话 | ✅ 支持 | ❌ 未实现 | 中 |
| 表情包 | ✅ 支持 | ❌ 未实现 | 中 |
| 截图功能 | ✅ 支持 | ❌ 未实现 | 低 |

#### 群组功能
| 功能 | 业界 IM 应用 | 当前项目状态 | 建议实现优先级 |
|------|-------------|-------------|----------------|
| 群管理（添加/移除成员） | ✅ 支持 | ❌ 未实现 | 高 |
| 群公告 | ✅ 支持 | ❌ 未实现 | 中 |
| 群昵称 | ✅ 支持 | ❌ 未实现 | 中 |
| 群权限设置 | ✅ 支持 | ❌ 未实现 | 中 |
| 群聊@功能 | ✅ 支持 | ❌ 未实现 | 高 |
| 群文件共享 | ✅ 支持 | ❌ 未实现 | 低 |

#### 用户功能
| 功能 | 业界 IM 应用 | 当前项目状态 | 建议实现优先级 |
|------|-------------|-------------|----------------|
| 个人动态/朋友圈 | ✅ 支持 | ❌ 未实现 | 中 |
| 好友申请与验证 | ✅ 支持 | ❌ 未实现 | 高 |
| 黑名单 | ✅ 支持 | ❌ 未实现 | 中 |
| 多设备登录 | ✅ 支持 | ❌ 未实现 | 低 |
| 深色模式 | ✅ 支持 | ❌ 未实现 | 中 |
| 个人二维码 | ✅ 支持 | ❌ 未实现 | 低 |

#### 系统功能
| 功能 | 业界 IM 应用 | 当前项目状态 | 建议实现优先级 |
|------|-------------|-------------|----------------|
| 推送通知 | ✅ 支持 | ❌ 未实现 | 高 |
| 网络状态检测 | ✅ 支持 | ❌ 未实现 | 中 |
| 数据备份与恢复 | ✅ 支持 | ❌ 未实现 | 低 |
| 隐私设置 | ✅ 支持 | ❌ 未实现 | 中 |
| 国际化支持 | ✅ 支持 | ❌ 未实现 | 低 |
| 错误重试机制 | ✅ 支持 | ❌ 未实现 | 中 |

### 组织层面缺失分析
#### 项目架构与代码组织
| 方面 | 业界最佳实践 | 当前项目状态 | 建议改进优先级 |
|------|-------------|-------------|----------------|
| 模块化程度 | 高（功能模块化，职责分离） | 中（MessageService 职责过重） | 高 |
| 状态管理 | 统一（如 Riverpod、Bloc） | 分散（ChangeNotifier + setState） | 高 |
| 导航管理 | 集中化（命名路由 + 导航服务） | 分散（直接使用 Navigator.push） | 高 |
| 错误处理 | 统一（Result 类型 + 全局处理） | 分散（try-catch 散落各处） | 高 |
| 测试覆盖 | 高（单元测试 + 集成测试 + UI 测试） | 低（基本无测试） | 中 |
| 代码规范 | 严格（lint 工具 + CI 检查） | 中（无统一规范） | 中 |

#### 开发与部署
| 方面 | 业界最佳实践 | 当前项目状态 | 建议改进优先级 |
|------|-------------|-------------|----------------|
| CI/CD 流程 | 完善（自动化构建、测试、部署） | 无 | 中 |
| 版本管理 | 规范（语义化版本 + 变更日志） | 无 | 低 |
| 性能监控 | 有（应用性能监控 + 崩溃分析） | 无 | 低 |
| 日志系统 | 完善（分级日志 + 远程日志） | 基础（本地日志） | 中 |
| 多端适配 | 支持（iOS、Android、Web、桌面） | 基础（仅移动端） | 低 |

#### 用户体验与设计
| 方面 | 业界最佳实践 | 当前项目状态 | 建议改进优先级 |
|------|-------------|-------------|----------------|
| 响应式设计 | 支持（适配不同屏幕尺寸） | 基础（仅移动端布局） | 中 |
| 动画效果 | 流畅（过渡动画 + 交互反馈） | 基础（简单动画） | 低 |
| 可访问性 | 支持（屏幕阅读器 + 键盘导航） | 无 | 低 |
| 主题定制 | 支持（浅色/深色模式 + 自定义主题） | 基础（仅浅色模式） | 中 |
| 加载状态 | 优化（骨架屏 + 渐进式加载） | 基础（简单加载动画） | 中 |

### 优先级建议
#### 高优先级功能（近期实现）
1. **核心聊天功能**：
   - 消息撤回
   - 消息转发
   - 消息已读回执
   - 群聊@功能

2. **多媒体功能**：
   - 图片发送
   - 视频发送
   - 语音消息

3. **用户功能**：
   - 好友申请与验证

4. **系统功能**：
   - 推送通知

5. **组织改进**：
   - 服务层拆分（MessageService 拆分为多个专注服务）
   - 统一状态管理（引入 Riverpod 或 Bloc）
   - 集中化导航管理（实现命名路由和导航服务）
   - 统一错误处理机制

#### 中优先级功能（中期实现）
1. **核心聊天功能**：
   - 消息引用
   - 消息置顶
   - 免打扰设置

2. **多媒体功能**：
   - 文件传输
   - 语音通话
   - 视频通话
   - 表情包

3. **群组功能**：
   - 群管理（添加/移除成员）
   - 群公告
   - 群昵称
   - 群权限设置

4. **用户功能**：
   - 个人动态/朋友圈
   - 黑名单
   - 深色模式

5. **组织改进**：
   - 数据层抽象（添加 Repository 层）
   - 测试覆盖（为核心功能添加单元测试）
   - 代码规范（配置 lint 工具）
   - 响应式设计（适配不同屏幕尺寸）

#### 低优先级功能（长期实现）
1. **核心聊天功能**：
   - 消息草稿

2. **多媒体功能**：
   - 截图功能

3. **群组功能**：
   - 群文件共享

4. **用户功能**：
   - 多设备登录
   - 个人二维码

5. **系统功能**：
   - 数据备份与恢复
   - 国际化支持

6. **组织改进**：
   - CI/CD 流程
   - 版本管理
   - 性能监控
   - 多端适配

## MVP 功能规划
### MVP 定义
MVP（Minimum Viable Product）是指最小可行产品，包含足够的核心功能，能够满足用户的基本需求，同时保持开发工作量可控。本规划基于当前项目已实现的功能，结合业界 IM 应用的核心功能，确定 Flutter Rust Demo 的 MVP 范围。

### 当前已实现功能
- ✅ 启动屏与自动登录
- ✅ 登录功能
- ✅ 会话列表展示
- ✅ 聊天功能
- ✅ 个人信息编辑
- ✅ 用户资料展示
- ✅ 通讯录功能
- ✅ 搜索功能

### MVP 核心功能规划
#### 核心聊天功能
| 功能 | 状态 | 实现优先级 | 备注 |
|------|------|------------|------|
| 文本消息发送 | ✅ 已实现 | - | 基础功能 |
| 消息列表展示 | ✅ 已实现 | - | 基础功能 |
| 消息撤回 | ❌ 未实现 | 高 | 核心功能，提升用户体验 |
| 消息已读回执 | ❌ 未实现 | 中 | 提升用户体验 |
| 消息置顶 | ❌ 未实现 | 低 | 可选功能 |

#### 多媒体功能
| 功能 | 状态 | 实现优先级 | 备注 |
|------|------|------------|------|
| 图片发送 | ❌ 未实现 | 高 | 核心功能，满足基本多媒体需求 |
| 语音消息 | ❌ 未实现 | 中 | 提升用户体验 |
| 视频发送 | ❌ 未实现 | 低 | 可选功能 |
| 文件传输 | ❌ 未实现 | 低 | 可选功能 |

#### 群组功能
| 功能 | 状态 | 实现优先级 | 备注 |
|------|------|------------|------|
| 群聊基本功能 | ✅ 已实现 | - | 基础功能 |
| 群聊@功能 | ❌ 未实现 | 高 | 核心功能，提升群聊体验 |
| 群管理（添加/移除成员） | ❌ 未实现 | 中 | 基本群管理功能 |
| 群公告 | ❌ 未实现 | 低 | 可选功能 |

#### 用户功能
| 功能 | 状态 | 实现优先级 | 备注 |
|------|------|------------|------|
| 个人信息编辑 | ✅ 已实现 | - | 基础功能 |
| 用户资料展示 | ✅ 已实现 | - | 基础功能 |
| 好友申请与验证 | ❌ 未实现 | 高 | 核心功能，建立社交关系 |
| 黑名单 | ❌ 未实现 | 低 | 可选功能 |

#### 系统功能
| 功能 | 状态 | 实现优先级 | 备注 |
|------|------|------------|------|
| 推送通知 | ❌ 未实现 | 高 | 核心功能，及时提醒用户 |
| 网络状态检测 | ❌ 未实现 | 中 | 提升用户体验 |
| 错误重试机制 | ❌ 未实现 | 中 | 提升应用稳定性 |

### MVP 技术实现规划
#### 短期规划（2-3 周）
| 任务 | 描述 | 优先级 | 时间估计 |
|------|------|--------|----------|
| 消息撤回功能 | 实现消息撤回功能，包括发送方和接收方的处理 | 高 | 3 天 |
| 图片发送功能 | 实现图片选择、压缩、发送功能 | 高 | 4 天 |
| 群聊@功能 | 实现群聊中的@功能，包括@提醒和列表 | 高 | 3 天 |
| 好友申请与验证 | 实现好友申请发送、接收、验证功能 | 高 | 4 天 |
| 推送通知 | 集成推送通知功能，确保消息及时提醒 | 高 | 3 天 |

#### 中期规划（1-2 周）
| 任务 | 描述 | 优先级 | 时间估计 |
|------|------|--------|----------|
| 消息已读回执 | 实现消息已读状态显示和回执功能 | 中 | 2 天 |
| 语音消息 | 实现语音录制、播放功能 | 中 | 3 天 |
| 群管理功能 | 实现群成员添加、移除功能 | 中 | 2 天 |
| 网络状态检测 | 实现网络状态监听和提示 | 中 | 1 天 |
| 错误重试机制 | 实现网络请求错误自动重试机制 | 中 | 1 天 |

#### 技术改进
| 任务 | 描述 | 优先级 | 时间估计 |
|------|------|--------|----------|
| 服务层拆分 | 将 MessageService 拆分为 AuthService、ChatService、UserService | 高 | 3 天 |
| 统一状态管理 | 引入 Riverpod 管理核心状态 | 高 | 2 天 |
| 集中化导航管理 | 实现命名路由和导航服务 | 高 | 1 天 |
| 统一错误处理 | 实现 Result 类型和全局错误处理 | 高 | 2 天 |

### MVP 验收标准
#### 核心功能验收
- ✅ 文本消息发送和接收
- ✅ 会话列表展示
- ✅ 个人信息编辑和展示
- ✅ 通讯录功能
- ✅ 搜索功能
- ✅ 消息撤回
- ✅ 图片发送
- ✅ 群聊@功能
- ✅ 好友申请与验证
- ✅ 推送通知

#### 技术验收
- ✅ 服务层拆分完成
- ✅ 统一状态管理实现
- ✅ 集中化导航管理实现
- ✅ 统一错误处理实现
- ✅ 代码规范符合要求
- ✅ 应用稳定性良好

### MVP 发布准备
#### 测试计划
- **单元测试**：覆盖核心服务和功能
- **集成测试**：验证模块间交互
- **UI 测试**：测试核心 UI 功能
- **性能测试**：确保应用响应流畅
- **兼容性测试**：确保在不同设备上正常运行

#### 发布流程
1. 完成所有 MVP 功能开发
2. 进行全面测试
3. 修复发现的问题
4. 准备发布版本
5. 内部测试
6. 发布 MVP 版本

### 后续迭代规划
MVP 发布后，根据用户反馈和业务需求，逐步添加以下功能：
1. **多媒体功能**：视频发送、文件传输
2. **高级聊天功能**：消息引用、免打扰设置
3. **群组功能**：群公告、群权限设置
4. **用户功能**：个人动态/朋友圈、深色模式
5. **系统功能**：数据备份与恢复、国际化支持

## 项目组织习惯
- **目录结构**：按功能模块组织文件，遵循单一职责原则
- **命名规范**：使用驼峰命名法，类名首字母大写，方法和变量名首字母小写
- **代码规范**：计划配置 flutter_lints 或 custom_lints，确保代码风格一致
- **文档注释**：为公共 API 和关键逻辑添加详细的文档注释
- **测试覆盖**：为核心服务和组件编写单元测试，验证模块间交互

## 编程风格
- **组件化**：将复杂屏幕拆分为更小的可复用组件
- **状态管理**：使用 Riverpod 管理核心状态，减少 setState 的使用
- **导航管理**：使用 go_router 实现声明式路由配置，支持嵌套路由和路由守卫
- **主题与样式**：扩展 ThemeData，添加自定义主题属性，将常用样式提取为常量
- **响应式设计**：使用 MediaQuery 和 LayoutBuilder 实现响应式布局
- **错误处理**：创建 Result 类型，统一处理异步操作的错误
- **Rust 集成**：将 Rust 代码按功能模块组织，优化 Dart-Rust 通信

## Riverpod 状态管理架构

### 状态划分原则

基于当前项目的 MessageService 职责，将状态按业务领域划分为独立的 Provider，每个 Provider 只负责单一职责：

| Provider 名称 | 职责 | 依赖关系 |
|--------------|------|----------|
| `authProvider` | 管理登录状态、当前用户信息、Token | 无 |
| `connectionProvider` | 管理 WebSocket 连接状态、连接质量 | 依赖 authProvider |
| `conversationProvider` | 管理会话列表、会话同步状态 | 依赖 connectionProvider |
| `messageProvider` | 管理消息列表、发送状态 | 依赖 conversationProvider |
| `userProfileProvider` | 管理用户资料缓存 | 依赖 authProvider |
| `typingProvider` | 管理正在输入状态 | 依赖 conversationProvider |
| `notificationProvider` | 管理推送通知、未读消息计数 | 依赖 messageProvider |

### 状态模型设计

#### 1. 认证状态 (AuthState)
```dart
@freezed
class AuthState with _$AuthState {
  const factory AuthState({
    @Default(false) bool isAuthenticated,
    String? userId,
    UserProfile? currentUser,
    String? token,
    @Default(false) bool isLoading,
    String? error,
  }) = _AuthState;
}
```

#### 2. 连接状态 (ConnectionState)
```dart
@freezed
class ConnectionState with _$ConnectionState {
  const factory ConnectionState({
    @Default(ConnectionStatus.disconnected) ConnectionStatus status,
    @Default(0) int retryCount,
    DateTime? lastConnectedAt,
    String? error,
  }) = _ConnectionState;
}

enum ConnectionStatus {
  disconnected,
  connecting,
  connected,
  reconnecting,
  error,
}
```

#### 3. 会话状态 (ConversationState)
```dart
@freezed
class ConversationState with _$ConversationState {
  const factory ConversationState({
    @Default([]) List<Conversation> conversations,
    @Default(false) bool isSyncing,
    @Default(0) int syncProgress,
    String? currentConversationId,
    String? error,
  }) = _ConversationState;
}
```

#### 4. 消息状态 (MessageState)
```dart
@freezed
class MessageState with _$MessageState {
  const factory MessageState({
    @Default({}) Map<String, List<Message>> messagesByConversation,
    @Default({}) Map<String, MessageSendStatus> sendingMessages,
    @Default(false) bool isLoadingMore,
    String? error,
  }) = _MessageState;
}
```

#### 5. 用户资料状态 (UserProfileState)
```dart
@freezed
class UserProfileState with _$UserProfileState {
  const factory UserProfileState({
    @Default({}) Map<String, UserProfile> profiles,
    @Default({}) Set<String> loadingUserIds,
    String? error,
  }) = _UserState;
}
```

### Provider 实现示例

#### 1. Auth Provider
```dart
// Provider 定义
final authProvider = StateNotifierProvider<AuthNotifier, AuthState>((ref) {
  return AuthNotifier(ref);
});

// Notifier 实现
class AuthNotifier extends StateNotifier<AuthState> {
  AuthNotifier(this.ref) : super(const AuthState());
  
  final Ref ref;
  late final AuthRepository _repository = ref.read(authRepositoryProvider);
  
  Future<void> login(String phone, String verifyCode) async {
    state = state.copyWith(isLoading: true, error: null);
    
    final result = await _repository.login(phone, verifyCode);
    
    result.fold(
      (error) => state = state.copyWith(
        isLoading: false,
        error: error.message,
      ),
      (user) => state = state.copyWith(
        isAuthenticated: true,
        userId: user.userId,
        currentUser: user,
        isLoading: false,
      ),
    );
  }
  
  Future<void> logout() async {
    await _repository.logout();
    state = const AuthState();
  }
  
  Future<void> refreshUserProfile() async {
    if (state.userId == null) return;
    
    final result = await _repository.getUserProfile(state.userId!);
    
    result.fold(
      (error) => state = state.copyWith(error: error.message),
      (user) => state = state.copyWith(currentUser: user),
    );
  }
}
```

#### 2. Connection Provider
```dart
final connectionProvider = StateNotifierProvider<ConnectionNotifier, ConnectionState>((ref) {
  return ConnectionNotifier(ref);
});

class ConnectionNotifier extends StateNotifier<ConnectionState> {
  ConnectionNotifier(this.ref) : super(const ConnectionState()) {
    // 监听认证状态，自动连接
    ref.listen(authProvider, (previous, next) {
      if (next.isAuthenticated && !previous?.isAuthenticated) {
        connect();
      } else if (!next.isAuthenticated && previous?.isAuthenticated) {
        disconnect();
      }
    });
  }
  
  final Ref ref;
  StreamSubscription? _connectionStream;
  
  Future<void> connect() async {
    if (state.status == ConnectionStatus.connecting) return;
    
    state = state.copyWith(status: ConnectionStatus.connecting);
    
    try {
      final client = ref.read(imClientProvider);
      await client.connect();
      
      _connectionStream = client.connectionStream.listen(
        (event) => _handleConnectionEvent(event),
        onError: (error) => _handleConnectionError(error),
      );
      
      state = state.copyWith(
        status: ConnectionStatus.connected,
        lastConnectedAt: DateTime.now(),
        retryCount: 0,
      );
    } catch (e) {
      _handleConnectionError(e);
    }
  }
  
  void disconnect() {
    _connectionStream?.cancel();
    ref.read(imClientProvider).disconnect();
    state = const ConnectionState();
  }
  
  void _handleConnectionEvent(ConnectionEvent event) {
    // 处理连接事件
  }
  
  void _handleConnectionError(dynamic error) {
    state = state.copyWith(
      status: ConnectionStatus.error,
      error: error.toString(),
      retryCount: state.retryCount + 1,
    );
    
    // 自动重连逻辑
    if (state.retryCount < 5) {
      Future.delayed(Duration(seconds: state.retryCount * 2), connect);
    }
  }
}
```

#### 3. Conversation Provider
```dart
final conversationProvider = StateNotifierProvider<ConversationNotifier, ConversationState>((ref) {
  return ConversationNotifier(ref);
});

class ConversationNotifier extends StateNotifier<ConversationState> {
  ConversationNotifier(this.ref) : super(const ConversationState()) {
    // 监听连接状态，连接成功后同步会话
    ref.listen(connectionProvider, (previous, next) {
      if (next.status == ConnectionStatus.connected &&
          previous?.status != ConnectionStatus.connected) {
        syncConversations();
      }
    });
  }
  
  final Ref ref;
  StreamSubscription? _conversationStream;
  
  Future<void> syncConversations() async {
    state = state.copyWith(isSyncing: true, syncProgress: 0);
    
    try {
      final client = ref.read(imClientProvider);
      final conversations = await client.getAllConversations();
      
      state = state.copyWith(
        conversations: conversations,
        isSyncing: false,
        syncProgress: 100,
      );
      
      // 监听会话更新
      _conversationStream?.cancel();
      _conversationStream = client.conversationStream.listen(_handleConversationEvent);
    } catch (e) {
      state = state.copyWith(
        isSyncing: false,
        error: e.toString(),
      );
    }
  }
  
  void selectConversation(String conversationId) {
    state = state.copyWith(currentConversationId: conversationId);
  }
  
  void _handleConversationEvent(ConversationEvent event) {
    // 处理会话更新事件
    final updatedConversations = [...state.conversations];
    // 更新逻辑...
    state = state.copyWith(conversations: updatedConversations);
  }
  
  @override
  void dispose() {
    _conversationStream?.cancel();
    super.dispose();
  }
}
```

#### 4. Message Provider
```dart
final messageProvider = StateNotifierProvider<MessageNotifier, MessageState>((ref) {
  return MessageNotifier(ref);
});

class MessageNotifier extends StateNotifier<MessageState> {
  MessageNotifier(this.ref) : super(const MessageState()) {
    // 监听当前会话变化，加载消息
    ref.listen(
      conversationProvider.select((s) => s.currentConversationId),
      (previous, next) {
        if (next != null && next != previous) {
          loadMessages(next);
        }
      },
    );
  }
  
  final Ref ref;
  final Map<String, StreamSubscription> _messageStreams = {};
  
  Future<void> loadMessages(String conversationId, {int count = 20}) async {
    try {
      final client = ref.read(imClientProvider);
      final messages = await client.getMessageList(
        conversationId: conversationId,
        count: count,
      );
      
      final updatedMap = Map<String, List<Message>>.from(state.messagesByConversation);
      updatedMap[conversationId] = messages;
      
      state = state.copyWith(messagesByConversation: updatedMap);
      
      // 监听新消息
      _listenToMessages(conversationId);
    } catch (e) {
      state = state.copyWith(error: e.toString());
    }
  }
  
  Future<void> sendMessage(String conversationId, String content) async {
    final tempId = 'temp_${DateTime.now().millisecondsSinceEpoch}';
    
    // 乐观更新：立即显示发送中状态
    final optimisticMessage = Message(
      id: tempId,
      senderId: ref.read(authProvider).userId!,
      content: content,
      timestamp: DateTime.now(),
      sendStatus: MessageSendStatus.sending,
    );
    
    _addMessageToState(conversationId, optimisticMessage);
    
    final sendingMap = Map<String, MessageSendStatus>.from(state.sendingMessages);
    sendingMap[tempId] = MessageSendStatus.sending;
    state = state.copyWith(sendingMessages: sendingMap);
    
    try {
      final client = ref.read(imClientProvider);
      final sentMessage = await client.sendMessage(
        conversationId: conversationId,
        content: content,
      );
      
      // 替换临时消息为实际消息
      _replaceMessage(conversationId, tempId, sentMessage);
      
      final updatedSending = Map<String, MessageSendStatus>.from(state.sendingMessages);
      updatedSending[tempId] = MessageSendStatus.sent;
      state = state.copyWith(sendingMessages: updatedSending);
    } catch (e) {
      // 标记发送失败
      final updatedSending = Map<String, MessageSendStatus>.from(state.sendingMessages);
      updatedSending[tempId] = MessageSendStatus.failed;
      state = state.copyWith(sendingMessages: updatedSending);
    }
  }
  
  Future<void> retrySendMessage(String conversationId, String messageId) async {
    // 重发逻辑
  }
  
  void _addMessageToState(String conversationId, Message message) {
    final updatedMap = Map<String, List<Message>>.from(state.messagesByConversation);
    final messages = updatedMap[conversationId] ?? [];
    updatedMap[conversationId] = [...messages, message];
    state = state.copyWith(messagesByConversation: updatedMap);
  }
  
  void _replaceMessage(String conversationId, String oldId, Message newMessage) {
    final updatedMap = Map<String, List<Message>>.from(state.messagesByConversation);
    final messages = updatedMap[conversationId] ?? [];
    final index = messages.indexWhere((m) => m.id == oldId);
    if (index != -1) {
      messages[index] = newMessage;
      updatedMap[conversationId] = [...messages];
      state = state.copyWith(messagesByConversation: updatedMap);
    }
  }
  
  void _listenToMessages(String conversationId) {
    _messageStreams[conversationId]?.cancel();
    
    final client = ref.read(imClientProvider);
    _messageStreams[conversationId] = client.messageStream.listen((message) {
      if (message.conversationId == conversationId) {
        _addMessageToState(conversationId, message);
      }
    });
  }
  
  @override
  void dispose() {
    for (final subscription in _messageStreams.values) {
      subscription.cancel();
    }
    super.dispose();
  }
}
```

#### 5. User Profile Provider
```dart
final userProfileProvider = StateNotifierProvider<UserProfileNotifier, UserProfileState>((ref) {
  return UserProfileNotifier(ref);
});

class UserProfileNotifier extends StateNotifier<UserProfileState> {
  UserProfileNotifier(this.ref) : super(const UserProfileState());
  
  final Ref ref;
  
  Future<UserProfile?> getUserProfile(String userId) async {
    // 先检查缓存
    if (state.profiles.containsKey(userId)) {
      return state.profiles[userId];
    }
    
    // 避免重复加载
    if (state.loadingUserIds.contains(userId)) {
      return null;
    }
    
    final loadingIds = Set<String>.from(state.loadingUserIds)..add(userId);
    state = state.copyWith(loadingUserIds: loadingIds);
    
    try {
      final client = ref.read(imClientProvider);
      final profiles = await client.getUsersInfo(userIds: [userId]);
      
      if (profiles.isNotEmpty) {
        final updatedProfiles = Map<String, UserProfile>.from(state.profiles);
        updatedProfiles[userId] = profiles.first;
        
        final updatedLoading = Set<String>.from(state.loadingUserIds)..remove(userId);
        
        state = state.copyWith(
          profiles: updatedProfiles,
          loadingUserIds: updatedLoading,
        );
        
        return profiles.first;
      }
    } catch (e) {
      final updatedLoading = Set<String>.from(state.loadingUserIds)..remove(userId);
      state = state.copyWith(
        loadingUserIds: updatedLoading,
        error: e.toString(),
      );
    }
    
    return null;
  }
  
  Future<void> preloadUserProfiles(List<String> userIds) async {
    final uncachedIds = userIds
        .where((id) => !state.profiles.containsKey(id))
        .where((id) => !state.loadingUserIds.contains(id))
        .toList();
    
    if (uncachedIds.isEmpty) return;
    
    final loadingIds = Set<String>.from(state.loadingUserIds)..addAll(uncachedIds);
    state = state.copyWith(loadingUserIds: loadingIds);
    
    try {
      final client = ref.read(imClientProvider);
      final profiles = await client.getUsersInfo(userIds: uncachedIds);
      
      final updatedProfiles = Map<String, UserProfile>.from(state.profiles);
      for (final profile in profiles) {
        updatedProfiles[profile.userId] = profile;
      }
      
      final updatedLoading = Set<String>.from(state.loadingUserIds)
        ..removeAll(uncachedIds);
      
      state = state.copyWith(
        profiles: updatedProfiles,
        loadingUserIds: updatedLoading,
      );
    } catch (e) {
      final updatedLoading = Set<String>.from(state.loadingUserIds)
        ..removeAll(uncachedIds);
      
      state = state.copyWith(
        loadingUserIds: updatedLoading,
        error: e.toString(),
      );
    }
  }
  
  void updateCurrentUserProfile(UserProfile profile) {
    final updatedProfiles = Map<String, UserProfile>.from(state.profiles);
    updatedProfiles[profile.userId] = profile;
    state = state.copyWith(profiles: updatedProfiles);
    
    // 同时更新 authProvider 中的当前用户信息
    final authState = ref.read(authProvider);
    if (authState.userId == profile.userId) {
      ref.read(authProvider.notifier).updateCurrentUser(profile);
    }
  }
}
```

### Repository 层 Provider

```dart
// Repository Provider
final authRepositoryProvider = Provider<AuthRepository>((ref) {
  return AuthRepositoryImpl(ref.read(imClientProvider));
});

final conversationRepositoryProvider = Provider<ConversationRepository>((ref) {
  return ConversationRepositoryImpl(ref.read(imClientProvider));
});

final messageRepositoryProvider = Provider<MessageRepository>((ref) {
  return MessageRepositoryImpl(ref.read(imClientProvider));
});

// IM Client Provider
final imClientProvider = Provider<OpenImBridgeClient>((ref) {
  // 单例模式
  return OpenImBridgeClient.instance;
});
```

### 在 UI 中使用

```dart
class ChatListScreen extends ConsumerWidget {
  @override
  Widget build(BuildContext context, WidgetRef ref) {
    // 监听会话状态
    final conversationState = ref.watch(conversationProvider);
    final connectionState = ref.watch(connectionProvider);
    
    // 选择性地监听特定状态变化
    final currentConversationId = ref.watch(
      conversationProvider.select((s) => s.currentConversationId)
    );
    
    return Scaffold(
      appBar: AppBar(
        title: Text('聊天'),
        actions: [
          // 显示连接状态
          if (connectionState.status == ConnectionStatus.connecting)
            const CircularProgressIndicator(),
        ],
      ),
      body: conversationState.isSyncing
          ? const Center(child: CircularProgressIndicator())
          : ListView.builder(
              itemCount: conversationState.conversations.length,
              itemBuilder: (context, index) {
                final conversation = conversationState.conversations[index];
                return ConversationListItem(
                  conversation: conversation,
                  isSelected: conversation.id == currentConversationId,
                  onTap: () {
                    ref.read(conversationProvider.notifier)
                        .selectConversation(conversation.id);
                    AppRouter.goToChatDetail(context, conversation);
                  },
                );
              },
            ),
    );
  }
}

class ChatDetailScreen extends ConsumerWidget {
  final String conversationId;
  
  const ChatDetailScreen({required this.conversationId});
  
  @override
  Widget build(BuildContext context, WidgetRef ref) {
    // 获取当前会话的消息
    final messageState = ref.watch(messageProvider);
    final messages = messageState.messagesByConversation[conversationId] ?? [];
    
    // 获取发送状态
    final sendingStatus = messageState.sendingMessages;
    
    return Scaffold(
      appBar: AppBar(title: Text('聊天详情')),
      body: Column(
        children: [
          Expanded(
            child: ListView.builder(
              itemCount: messages.length,
              itemBuilder: (context, index) {
                final message = messages[index];
                return MessageBubble(
                  message: message,
                  sendStatus: sendingStatus[message.id],
                );
              },
            ),
          ),
          ChatInput(
            onSend: (content) {
              ref.read(messageProvider.notifier)
                  .sendMessage(conversationId, content);
            },
          ),
        ],
      ),
    );
  }
}
```

### 状态管理最佳实践

1. **单一职责**：每个 Provider 只负责一个业务领域的状态
2. **依赖注入**：使用 Riverpod 的依赖注入管理 Repository 和 Service
3. **选择性监听**：使用 `select` 减少不必要的重建
4. **状态不可变**：使用 Freezed 生成不可变状态类
5. **错误处理**：统一使用 Result 类型处理异步操作错误
6. **自动清理**：在 dispose 中取消 Stream 订阅，避免内存泄漏
7. **乐观更新**：发送消息时立即更新 UI，提升用户体验
8. **缓存策略**：用户资料等数据合理缓存，减少网络请求

## 参考资源
- [Flutter 官方文档](https://flutter.dev/docs)
- [Riverpod 文档](https://riverpod.dev/)
- [Bloc 文档](https://bloclibrary.dev/)
- [Flutter Rust Bridge 文档](https://github.com/fzyzcjy/flutter_rust_bridge)
- [Flutter 性能优化指南](https://flutter.dev/docs/perf/rendering)
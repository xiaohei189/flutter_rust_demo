{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug im_client",
            "type": "lldb",
            "request": "launch",
            "cargo": {
                "args": ["build", "--bin=im_client"],
                "filter": { "name": "im_client", "kind": "bin" }
            },
            "args": [],
            "cwd": "${workspaceFolder}",
            "terminal": "integrated",
            "sourceLanguages": ["rust"]
        }
    ]
}

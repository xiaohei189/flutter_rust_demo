// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.11.1.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import '../frb_generated.dart';
import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';

// These functions are ignored because they are not marked as `pub`: `build_url`, `get_content_type_name`, `handle_binary_message`, `handle_messages`, `handle_push_message`, `is_duplicate_message`, `parse_content`, `print_msg_data`, `send_request`
// These function are ignored because they are on traits that is not defined in current crate (put an empty `#[frb]` on it to unignore): `clone`, `clone`, `fmt`

// Rust type: RustOpaqueMoi<flutter_rust_bridge::for_generated::RustAutoOpaqueInner<OpenIMClient>>
abstract class OpenImClient implements RustOpaqueInterface {
  /// 连接到服务器并在内部启动消息处理
  Future<void> connect();

  // HINT: Make it `#[frb(sync)]` to let it become the default constructor of Dart class.
  /// 创建新的客户端
  /// - `config`: 客户端配置
  static Future<OpenImClient> newInstance({required ClientConfig config}) =>
      RustLib.instance.api.crateApiClientOpenImClientNew(config: config);

  /// 发送文本消息
  Future<void> sendTextMessage({
    required String recvId,
    required String text,
    required int sessionType,
  });
}

/// 客户端配置
class ClientConfig {
  /// 用户 ID
  final String userId;

  /// 认证 token
  final String token;

  /// 平台 ID
  final int platformId;

  /// WebSocket 服务器 URL
  final String wsUrl;

  /// 压缩方式，例如 "gzip" 或空字符串表示不压缩
  final String compression;

  /// 是否为后台模式
  final bool isBackground;

  /// 是否需要消息响应
  final bool isMsgResp;

  /// SDK 类型，例如 "js" 或 "go"
  final String sdkType;

  const ClientConfig({
    required this.userId,
    required this.token,
    required this.platformId,
    required this.wsUrl,
    required this.compression,
    required this.isBackground,
    required this.isMsgResp,
    required this.sdkType,
  });

  // HINT: Make it `#[frb(sync)]` to let it become the default constructor of Dart class.
  /// 创建默认配置
  static Future<ClientConfig> newInstance({
    required String userId,
    required String token,
    required int platformId,
  }) => RustLib.instance.api.crateApiClientClientConfigNew(
    userId: userId,
    token: token,
    platformId: platformId,
  );

  @override
  int get hashCode =>
      userId.hashCode ^
      token.hashCode ^
      platformId.hashCode ^
      wsUrl.hashCode ^
      compression.hashCode ^
      isBackground.hashCode ^
      isMsgResp.hashCode ^
      sdkType.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ClientConfig &&
          runtimeType == other.runtimeType &&
          userId == other.userId &&
          token == other.token &&
          platformId == other.platformId &&
          wsUrl == other.wsUrl &&
          compression == other.compression &&
          isBackground == other.isBackground &&
          isMsgResp == other.isMsgResp &&
          sdkType == other.sdkType;
}
